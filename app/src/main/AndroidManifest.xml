<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prodent"
        tools:targetApi="31">
        <activity android:name=".view.HomePacienteActivity"/>
        <activity android:name=".view.HorarioActivity" />
        <activity android:name=".view.HomeDoctorActivity" />
        <activity android:name=".view.RegistroActivity" />
        <activity android:name=".view.RegistrarCitaActivity" />
        <activity android:name=".view.ConfiguracionActivity" />
        <activity android:name=".view.PerfilActivity" />
        <activity android:name=".view.CitasPacienteActivity" />
        <activity android:name=".view.MapaActivity" />
        <activity android:name=".view.NotificacionesActivity" />
        <activity android:name=".view.IndicacionesActivity" />
        <activity android:name=".view.ResenasBottomSheetFragment" />
        <activity
            android:name=".view.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>